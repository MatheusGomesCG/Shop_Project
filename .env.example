# Copie para .env (que o git ignora). Valores de desenvolvimento local, nunca de servidor.
POSTGRES_DB=foodcenture
POSTGRES_USER=foodcenture
POSTGRES_PASSWORD=foodcenture
# HS256 exige no mínimo 32 bytes.
JWT_SECRET=dev-only-troque-fora-da-maquina-local-0123456789abcdef
PAYMENT_GATEWAY_MODE=stub
DELIVERY_SIMULATOR_INTERVAL_MS=5000
